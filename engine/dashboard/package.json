{
  "name": "apm-dashboard-server",
  "version": "2.0.0",
  "description": "QuorumKit Dark Factory — real orchestrator backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
